# Chat Session — 2026-05-30

## Summary
- Named new pipeline component: **Token Sense Resolution (TSR)**
- Fixed grep/glob tool Expand-Archive module loading issue
- Clarified architecture differences: TSR vs ContextualGloss vs LITE pipeline
- Analyzed legacy Handler C code

## Key Discussion Points

1. **Naming**: Chose "Token Sense Resolution" over Handler C or Token Sense Disambiguation
2. **Architecture clarification**: Three layers produce English output:
   - TSR (build-time): picks sense_index → feeds InflectionEngineService COALESCE → gloss_set_entry
   - ContextualGloss (read-time): separate overlay for 13 polysemous words, stored in contextual_gloss table
   - LITE pipeline (build-time): generates gloss_set_entry for all 137K tokens
3. **Legacy Handler C**: Direct DB write violation (psycopg2 → token_sense_override), no admin endpoint to reuse
4. **Pipeline order**: Handler A (morph keys) → Handler B (COALESCE) → Handler C/TSR (sense selection) → Handler D (LITE gloss generation)
5. **Tool fix**: Added WindowsApps PS7 module path to Machine-level PSModulePath + installed PSGallery Microsoft.PowerShell.Archive module

## Next Steps
1. Build Java POST /admin/import-sense-selections endpoint for TSR JSONL import
2. Write tests for existing sense override controllers
3. Update DESIGN.md with TSR pipeline
4. Run full NT batch and review quality
5. Fix G3588 duplicate senses
